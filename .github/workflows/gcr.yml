name: Push to GCR GitHub Action
on:
  push:
    branches:
        - "test"
        - "master"
jobs:
  build-and-push-to-gcr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Extracting Build Environment and Build Id
        id: gitinfo
        run: |
          build_env=${GITHUB_REF#refs/heads/}
          build_id=$(git rev-parse --short HEAD)
          echo "::set-output name=build_env::${build_env}"
          echo "::set-output name=build_id::${build_id}"
          echo Build Environment=${build_env}
          echo Build Id=${build_id}
      
      - name: Preparing Secret Keys
        id: secretkeys
        run: |
          env=${{steps.gitinfo.outputs.build_env}}
          env=${env^^}
          echo $env
          echo ${{steps.gitinfo.outputs.build_id}}

      # - uses: google-github-actions/setup-gcloud@master
      # - uses: RafikFarhad/push-to-gcr-github-action@v4
      #   with:
      #     gcloud_service_key: ${{ secrets.GCLOUD_SERVICE_KEY }}
      #     registry: gcr.io
      #     project_id: ${{ secrets.GCLOUD_PROJECT_ID }}
      #     image_name: ${{ secrets.GCLOUD_IMGAGE_NAME }}
      #     image_tag: ${{steps.gitinfo.outputs.build_env}}
      #     dockerfile: Dockerfile